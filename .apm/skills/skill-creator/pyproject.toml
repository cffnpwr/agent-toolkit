[project]
name = "skill-creator"
version = "0.1.0"
description = "Bundled Python scripts for the skill-creator skill"
requires-python = "==3.14.6"
dependencies = [
    "pyyaml==6.0.3",
]

[tool.uv]
package = false
