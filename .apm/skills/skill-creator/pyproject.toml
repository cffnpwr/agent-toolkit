[project]
name = "skill-creator"
version = "0.1.0"
description = "Bundled Python scripts for the skill-creator skill"
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0",
]

[tool.uv]
package = false
