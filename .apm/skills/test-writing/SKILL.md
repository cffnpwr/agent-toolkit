---
name: test-writing
description: テストを書く・既存テストをレビュー/改善する際の一貫した方針を、対象言語に応じて示すガイド。粒度（テストピラミッド）・何をテストし何をしないか（public経路はC1、ロジックを持つprivate、単純コード・IOの扱い）・命名・構造（Given-When-Then）・モックの使いどころといった言語非依存の核を与え、言語を判定して rust / go / typescript の固有手順（ランナー・配置・パラメタライズ・カバレッジ・境界モック）へ導く。次のときに使う。(1) 新しいテストを書く、(2)「テストを書いて」「テストを追加して」「ユニットテストを足して」と言われた、(3) 既存テストをレビュー・改善したい、(4)「テストが薄い」「テストの粒度を見直して」と言われた、(5) 他者やサブエージェントが書いたテストを点検する、(6) テストに関するレビュー指摘を受け取り採否を判断する、(7)「C1を満たして」等、カバレッジの目標や測り方を問われたとき、(8) 既存テストを別の書き方へ移す（アサーションの道具の入れ替え・構造の作り直し）。(5)〜(8) は自分がテストを書く場面でなくても該当する。単なるテスト実行だけの依頼では使わない。
---

# Test Writing

テストの書き方を、言語非依存の方針と言語固有の手順に分けて示すガイド。

## 概要

テストの粒度・何をテストするか・命名・構造・モックの使いどころは言語を横断して共有できる。このスキルはその核を本文に置き、フレームワーク固有の具体手順（ランナー・配置・パラメタライズ・カバレッジ・境界モック）を `references/<言語>.md` に分ける。テストの新規執筆と既存テストのレビュー/改善の両方に適用する。

## 最初にすること

1. 対象コードと依頼から言語を判定する。
2. 該当する reference を読む。
   - Rust → [references/rust.md](references/rust.md)
   - Go → [references/go.md](references/go.md)
   - TypeScript → [references/typescript.md](references/typescript.md)
   - 一覧に無い言語 → reference は無い。本文の言語非依存方針のみを適用し、命名・配置・カバレッジ計測などの具体手順は対象言語の標準テストランナーの公式ドキュメントとプロジェクトの既存慣行に従う。
3. 本文の言語非依存方針と、reference の言語固有手順を合わせて適用する。

言語固有の道具（アサーション・モック・カバレッジ計測）は、対象プロジェクトの既存慣行があればそれを優先し、無ければ reference の定番に従う。

## テスト方針（言語非依存の核）

### 粒度

テストピラミッドに従う。高速・安定な unit テストを土台に多数置き、integration は要所に絞り、e2e はごく少数にする。

### 何をテストし、何をしないか

- **public な呼び出し経路は C1（分岐網羅）を満たすようにテストを書く。** 各条件分岐の真・偽の双方を通すケースを用意する。
- **C1 の対象は到達可能な分岐に限る。** 到達不能・防御的な分岐（`unreachable!`、網羅 `match` の default 腕、実質起こり得ないエラー側など）は対象外とする。それらを通すためだけに注入点やモックを増設しない（後述「モック / テストダブル」の境界のみダブル化を優先）。可能なら型で分岐自体を消す。
- **private でも、ロジックを持つ内部要素はテストする。** 単なる委譲・受け渡しでなく判断や計算を含むものが対象。
- **単純なコードはテストしない。** 自明な getter・素通しのラッパ等はケースを起こさない。
- **IO のようにテストが難しいものは無理にテストしない。** 外部境界そのものの検証にコストをかけず、境界を跨ぐロジック側をテスト可能な形に寄せる。
- **副作用を持つ実処理を、テストが実行しない構造を保つ。** ファイル作成・外部コマンド実行・ネットワークアクセス等の実処理の呼び出しは、差し替え可能な継ぎ目の向こうに置く。引数解釈・組み立て・分岐の検証は、その継ぎ目を跨がないようにする。実処理が未実装だから安全、という状態に依存しない（実装された瞬間に副作用が出る）。設計変更で継ぎ目が消えたら、同じ保証を別の層で立て直す。
- **テストの入力に、実環境のファイルや本番の設定データを使わない。** フィクスチャを用意して読ませる。実データでは代表・境界ケースをテスト側で表現できず、テストと無関係なデータ編集でテストの結果が動く（リポジトリにコミットされたデータでも同じ）。
  - フィクスチャは実データから代表的な形を写し、**どの実ファイルのどの形を写したかを残す**（フィクスチャ内のコメント・近接する README 等）。
  - 実データ自体が読めることを守りたい場合は、ユニットテストに混ぜず、別の検査（スキーマ検証・CI での読み込み確認）として分ける。
- **テスト対象は自分が実装したものに限る。** 依存ライブラリ・基盤ミドルウェアの機能は検証しない。
- **確率的に失敗するテストを置かない。** 外部要因で失敗した場合は、失敗条件を再現し、テストと実装の問題でないことを示す。

### 命名

検証する条件と期待結果が名前から読めることを原則とする。正常系・異常系を prefix で区別する。

- **テストケースごとに関数を書く言語（例: Rust）**: `positive` / `negative` を prefix に付け、以降は言語の命名慣習に従う。
- **テストケースを文字列等で自由記述できる言語（例: Go, TypeScript）**: `[positive] ` / `[negative] ` を prefix に付ける。以降は「A が B のとき C（となる）」のように入力条件と期待結果がわかるよう、文の主題を明確にして書く。

### 構造

Given-When-Then を基本パターンとする（準備 → 実行 → 検証）。

- **BDD スタイル（describe/it 等）の場合**: テスト対象ごとに `describe` を分ける。
- **テーブルドリブン（TDT）の場合**: テスト対象ごとにテスト関数を分ける。ケースを表す構造体は次を守る。
  - **他のフィールドから導出できるフィールドを置かない。** 期待するエラーの詳細（エラー値・エラー型）を持つテーブルに、「失敗するか」の真偽値を併置しない。詳細フィールドが非 nil であることは失敗の条件そのものなので、真偽値は導出可能で冗長になる。真偽値を使うのは、エラーの種別を検査できない本番コードが相手で詳細フィールドを置けない場合に限る。
  - **1 つの概念を複数のフィールドに割らない。** テストダブルが返す結果は、ダブル側が持つ結果型をそのままフィールドに置く。「返す値」と「失敗させるか」のように 1 つの結果を複数フィールドへ割ると、同じことを表す道具がテスト内で二重になる。
- **Given-When-Then の各節に、そのラベルを付ける。**
- **重複をテスト側で圧縮しない。** 同じ定義が複数箇所にあることが原因なら、定義側で共通化する。

### 検証（アサーション）

- **正常系は期待値全体との完全一致で検証する。** 期待値のオブジェクト / 構造体を 1 つ組み立て、結果全体を一度に突き合わせる。フィールドごとの部分検証はしない（アサーション行が増え、検査対象から漏れたフィールドの差分に気づけないため）。
- **異常系はエラーの型・種別まで検証する。** 期待するエラー型 / バリアントに一致することを検証する。**エラーメッセージの文言は検証しない**（実装変更で容易に壊れ、検証の意図も型ほど明確でないため）。
- **同じ判定目的に、複数のアサーションの道具を使わない。** 1 つの目的（値の比較・エラー検査等）に複数のライブラリや標準機能が混ざっている状態は、道具を 1 つに寄せる。目的の異なる道具の併用（深い比較と単純な値の比較で使い分ける等）は混在に当たらない。既存慣行がある場合は「最初にすること」の既存慣行優先に従い、統一が依頼範囲を超えるなら指摘に留める。

型・種別で検証するには、本番コード側がそれを可能にしている必要がある。テスト側の作法として守るのでなく、次を設計への要求として扱う。本番コードがこの形でない場合、その変更が依頼範囲に含まれるかを判断し、含まれなければ指摘に留めて、現状の実装で検証できる範囲までテストを書く（本番コードの修正をテスト着手の前提にしない）。

- **呼び出し元が分岐しうるエラーには、独自エラー型か sentinel を用意する。** 素の文字列整形エラーしか返さない実装では、テストにできるのは「エラーが起きた」の確認だけになる。
- **診断のための情報（コマンド名・終了コード・ファイルパス・フィールド名・許容値など）は、メッセージ文字列でなくエラー型のフィールドに持たせる。** メッセージは人間向けの表示に限り、検証の対象にしない。情報をメッセージにしか持たせないと、検証したいときに文言の突き合わせへ流れる。
- **依存するライブラリの API を選ぶときも、返るエラーを型・種別で検査できるかを判断材料に入れる。** 非公開の sentinel を包んで返す API は、この方針では検査できない。同じ目的の別の API を選ぶか、検査したい判断を自分の純粋関数へ切り出し、型付きエラーを返す形にする。

### モック / テストダブル

外部境界（ネットワーク・DB・時刻・乱数・外部コマンド・ファイルシステムなど）のみ必要最小限でダブルに置き換える。内部モジュール同士はモックせず実物で繋ぐ。境界を差し替えられない場合は、依存を境界へ寄せる設計変更を行う（その変更が依頼範囲を超えるなら指摘に留める）。

呼び出しを記録するダブルを使う場合、記録の検証は次の形にする。

- **記録した呼び出しは全件・順序込みで期待値と突き合わせる。** 呼ばれたことだけを見るテストにしない。引数に加えて境界へ渡す入力（標準入力・リクエストボディなど）も比較対象に含めると、境界へ渡すために組み立てた生成物がそのまま検証対象になる。
- **途中で失敗する異常系は、そこまでの呼び出し列を期待値にする。** 期待する呼び出し列を短く書くだけで、失敗した経路から先を実行しないことが担保される。

## カバレッジ

### C1 を求められたときの手順

「C1 を満たして」「カバレッジを上げて」と言われたら、次の順で進める。機械計測できない言語でも同じ手順で担保する。

1. 対象関数の条件分岐を列挙する。
2. 各分岐の真・偽を通すケースが既存テストにあるかを対応表にし、応答内に示す（ファイルには残さない）。
3. 欠けているものを足す。
4. 到達不能・防御的な分岐は、理由を添えて対象外とする。
5. 足したケースが実際に効いていることを、「検証が効いていることの確認（変異）」の手順で見る。テーブルの 1 キーを本番コードから落としてもテストが 1 件も落ちない状態は、文カバレッジ 100% のまま成立する。

- **文カバレッジ（C0）の数値を目標にしない。** C1 を満たした結果として上がることはあるが、指標として別物である。数値表を渡して「この数字を上げろ」と指示する進め方を取らない。
- **数値を上げるためにケースの意味を薄めない。** 呼ばれたことだけを見るテスト、期待値を実装から写しただけのテストを足さない。

### 計測の言語差（注意）

C1（分岐網羅）を機械計測できるかは言語・ランナーで異なる。計測できない場合は、上の手順に沿って人手で担保する。詳細は各 reference を参照する。

| 言語 / ランナー | C1（分岐）の機械計測 |
| --- | --- |
| Rust (`cargo-llvm-cov`) | region coverage が既定。真の分岐カバレッジは nightly + `--branch`（unstable） |
| Go (`go test -cover`) | 不可（文カバレッジのみ）。ケースで人手担保 |
| TypeScript (`vitest`) | 可（`--coverage`） |
| TypeScript (`bun test`) | 不可（分岐未対応）。ケースで人手担保 |

## 検証が効いていることの確認（変異）

既存テストを別の書き方へ移したとき（アサーションの道具の入れ替え・構造の作り直しなど）と、C1 のためにケースを足したときは、**検証内容が実際に効いていることを確かめる。**

- **カバレッジは根拠にならない。** アサーションを消しても同じ文は実行されるため、書き換えの前後でカバレッジは変わらない。「その文が実行された」ことしか示さず、「その値が検証された」ことを示さない。
- **本番コードへ意図的な誤りを入れ、対象のテストが落ちることを見る。** 落ちなければ、その検証は失われているか、最初から効いていない。誤りは 1 つずつ入れ、確認後は必ず元へ戻し、テストが再び通ることを見る。
- **変異の選び方**: 分岐条件の反転、定数の書き換え、コレクション（テーブルのキー・返す要素）からの削除、早期 return の無効化。
- **安全に関わる検査（境界の外への書き出し、権限、入力の検証）は、検査そのものを無効化して落ちることを見る。** 値の誤りの注入だけでは、検査が丸ごと消えた場合を捉えられない。実行環境が安全検査の無効化をブロックする場合は、黙って飛ばさず、確認できなかった検査を報告する。
