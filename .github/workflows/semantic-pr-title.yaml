name: Semantic PR title
on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - edited
      - reopened
      - synchronize

permissions: {}

jobs:
  semantic-pr-title:
    permissions:
      pull-requests: read
    uses: cffnpwr/actions/.github/workflows/semantic-pr-title.yaml@327f481be143f44471d3844d2609f7fc623b001b # main
